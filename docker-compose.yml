services:
  vibe-blog:
    build:
      context: .
      args:
        - GEMINI_API_KEY=${GEMINI_API_KEY}
    image: vibe-blog:latest
    container_name: vibe-blog-prod
    ports:
      - "8080:80"
    restart: unless-stopped
