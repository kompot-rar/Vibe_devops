steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build

      # --- DIAGNOSTYKA: Zobaczmy co powstało w folderze dist ---
      - name: Debug Build Output
        run: |
          echo "📂 Zawartość folderu dist:"
          ls -R dist/
      # ---------------------------------------------------------

      - name: Clean Nginx Folder
        run: rm -rf /var/www/html/*

      - name: Deploy to Nginx
        run: |
          echo "🚀 Kopiowanie plików..."
          # Kopiujemy rekurencyjnie (-r) zawartość dist do html
          cp -r dist/. /var/www/html/
          # Zmiana właściciela na www-data (dla pewności)
          chown -R www-data:www-data /var/www/html
